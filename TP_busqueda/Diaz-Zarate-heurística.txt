En este trabajo se presenta como posible heurística utilizar el mínimo camino posible para agarrar cada
una de las frutas (nos inspiramos en el conocido problema TSP, "Travelling Salesman Problem").
El algoritmo es explicado aquí:
https://www.youtube.com/watch?v=XaXsJJh-Q5Y

Esta heurística es usada tanto en el cornersProblem como en el foodProblem (solo se manipulan los states de
manera de poder tener la posición del pacman y las coordenadas de las frutas) ya que al calcular los caminos
mínimos para recorrerlas, resulta indiferente la ubicación en la que estén las frutas.

La función de nuestra heurística, al devolver el mínimo camino posible desde la pacman hacia cada una
de las frutas, siempre retorna un número que no subestima el camino más corto desde el estado a la meta
del problema, ya que en la realidad el pacman tiene varias limitaciones como ser paredes y otros obstáculos que
hacen que el camino para llegar a la meta sea más largo. Además, en el caso de que no tuviera dichos
obstáculos, nuestra función devuelve el camino más corto así que en el peor caso, sería igual.
Por lo tanto la heurística es admisible.

Además, si recordamos la definición de heurística consistente, tenemos que ver que:
h(N) <= c(N,S) + h(S)
donde h es la función heurística, N es un nodo del grafo, S es un sucesor de N y c(N,S)
es el costo de ir desde N a S.

El coste de llegar del nodo N a un sucesor S es c(N, S) = 1 
dado que consideramos el costo del camino como su longitud 
(sin tener en cuenta fantasmas ni premios intermedios).
Con lo cual la verificación de consistencia será verificar
h(N) <= h(S) + 1        *alpha*


Tendremos 2 casos para S:
    -S está en dirección a favor del camino del menor costo, por lo tanto h(N) - 1 = h(S), lo cual verifica *alpha*
    -S está en dirección opuesta al camino del menor costo, lo cual puede tener 2 resultados posibles:
        -El camino del menor costo es volver a N y luego el camino desde N: en este caso h(N) + 1 = h(S), lo cual verifica *alpha*
        -El camino del menor costo es distinto a volver a N: En este caso demostremos por el absurdo, suponiendo que h(N) > h(S) + 1.
         Eso significaría que desde N el camino más corto es más largo que pasar por S y luego recorrer el camino más corto desde S.
         Lo cual es contradictorio ya que entonces el camino más corto desde N sería pasando por S y luego el camino más corto desde S, 
         Esta contradicción parte de suponer h(N) > h(S) + 1 y por lo tanto concluimos *alpha*

Por lo tanto la heurística es consistente (monótona).

Nota: dejamos un assert en la función de búsqueda A* donde se verifica efectivamente
la monotonía en cada paso.
